// enums are the structure that contains enumerated value 

enum grade {
    a,
    b,
    c,
    d
} 

console.log(grade.a) // will give 0 because enums are by default initialized with 0,1,2,3......

